#include "main.h"

/**
* mul - multiplies two integers.
* @a: first integer
* @b: second integer
*
* Return: multiplication of two integers
*/

int mul(int a, int b)
{
int res = (a * b);
return (res);
}
